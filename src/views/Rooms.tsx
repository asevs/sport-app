import * as React from 'react';
import RoomsTemplate from '../templates/RoomsTemplate';
const Rooms: React.FunctionComponent = () => (
  <RoomsTemplate>Rooms</RoomsTemplate>
);

export default Rooms;
